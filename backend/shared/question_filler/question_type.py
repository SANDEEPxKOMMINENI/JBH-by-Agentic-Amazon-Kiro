"""
Question types for form processing
Adapted from v1 for v2
"""


class QuestionType:
    """Constants for different question types in job application forms"""

    INPUT = "input"
    MULTI_LINE_INPUT = "multi_line_input"
    SELECT = "select"
    MULTI_SELECT = "multi_select"
    RADIO = "radio"
    UNKNOWN = "unknown"
